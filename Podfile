# Uncomment the next line to define a global platform for your project
 platform :ios, '9.0'

target 'Florit' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Florit
  
  pod 'Alamofire', '~> 4.4'
  pod 'SwiftyJSON'
  pod 'SDWebImage'

  target 'FloritTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FloritUITests' do
    # Pods for testing
  end

end
